# data

Generated only. Nothing here is committed except `.gitkeep` placeholders.

```
data/
├── raw/         csi.csv, video, frame timestamps, session.json  (recorded)
├── processed/   labels.csv, dataset.npz                          (labeled)
├── models/      trained model files
└── results/     evaluation output, confusion matrices, plots
```

Folder naming and file formats: [../docs/DATA_FORMAT.md](../docs/DATA_FORMAT.md)
